<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace = "dev.mvc.sms.SMSDAOInter">

<!-- 문자 받은 정보 생성  -->
  <insert id="proc" parameterType="dev.mvc.sms.SMSVO">
    INSERT INTO sms (smsno, memberno, ip, authno, mdate)
    VALUES(sms_seq.nextval, 1, #{ip}, #{authno}, sysdate)
  </insert>
  
  <select id="sms_list" resultType="dev.mvc.sms.SMSVO">
    SELECT smsno, memberno, ip, authno, mdate
    FROM sms
    ORDER BY smsno ASC, mdate ASC
  </select>



</mapper>