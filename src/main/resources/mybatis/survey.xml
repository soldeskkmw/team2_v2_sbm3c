<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper 
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<!-- survey.xml과 연동되는 Spring interface "dev.mvc.survey.SurveyDAOInter" 선언 --> 
<mapper namespace="dev.mvc.survey.SurveyDAOInter">
  <insert id="survey_create" parameterType="dev.mvc.survey.SurveyVO">
   INSERT INTO survey(surveyno, surveytopic, startdate, enddate, yn, rdate)
   VALUES (notice_seq.nextval, #{surveytopic}, #{startdate}, #{enddate}, #{yn}, sysdate)
  </insert> 
  
  <!-- 
  전체 목록, 출력 순서 반영
  레코드 갯수 만큼 SurveyVO 객체를 만들어 ArrayList에 저장하여 리턴,
  ArrayList<SurveyVO> list = new ArrayList<SurveyVO>(); 
  -->
  <select id="survey_list" resultType="dev.mvc.survey.SurveyVO">
    SELECT surveyno, surveytopic, startdate, enddate, yn, rdate
    FROM survey
    ORDER BY surveyno ASC
  </select>
  
  <!-- 조회, id: read, 입력: noticeno, 리턴: NoticeVO -->
  <select id="survey_read" resultType="dev.mvc.survey.SurveyVO" parameterType="int">
    SELECT surveyno, surveytopic, startdate, enddate, yn, rdate
    FROM survey
    WHERE surveyno = #{surveyno}
  </select>  
  
    <!-- 수정 처리 -->  
  <update id="survey_read_update" parameterType="dev.mvc.survey.SurveyVO">
    UPDATE survey
    SET surveytopic=#{surveytopic}, startdate=#{startdate}, enddate=#{enddate}
    WHERE surveyno=#{surveyno}
  </update>  
  
  <!-- 삭제, return: int -->
  <delete id="survey_read_delete" parameterType="int">
    DELETE FROM survey
    WHERE surveyno = #{surveyno}
  </delete>   
       
</mapper>